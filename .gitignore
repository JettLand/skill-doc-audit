# Agent 记忆目录（非技能产物，不纳入版本管理）
.workbuddy/

# 操作系统 / 编辑器杂项
Thumbs.db
Desktop.ini
.DS_Store
*.tmp

# 本地临时产物，不进版本库
backups/
__pycache__/
*.pyc

# 回归测试夹具（脏样本基线，self_validate.py 的输入，必须随仓库分发以保证 clone 后自校验可用）
# 注：tests/examples/ 的黄金快照(*.expected.json)与 manifest.json 已纳入版本管理
