# Agent 记忆目录（非技能产物，不纳入版本管理）
.workbuddy/

# 操作系统 / 编辑器杂项
Thumbs.db
Desktop.ini
.DS_Store
*.tmp

# 本地临时产物，不进版本库
backups/
__pycache__/
*.pyc

# 发布制品（生成产物，不进版本库）：由 post-commit 钩子经 sync_deploy.py 自动
# build_dist.ensure_fresh() 重建，保证部署副本 / SkillHub 发布永远基于最新 src，
# 杜绝陈旧 zip 漂移。磁盘文件保留，clone 后首次提交即重建。
src/dist/skill-doc-audit.zip

# 回归测试夹具（脏样本基线）：由 make_fixtures.py 声明式 recipe 生成，不进版本库
# （git checkout 后由 self_validate.py 自动重建，或手动 `python src/scripts/make_fixtures.py`）
tests/fixtures/
# 注：tests/examples/ 的黄金快照(*.expected.json)与 manifest.json 必须纳入版本管理（断言基线，不可自动重建以免削弱回归护栏）

# 市场质量基准实测器运行时缓存（质量索引 / 采样历史 / 下载的技能 zip / 报告），不进版本库
bench/
