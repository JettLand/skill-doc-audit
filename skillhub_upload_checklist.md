# skill-doc-audit 上架 SkillHub 清单

> 状态：已就绪待上传。本文件为上传前核对与表单草稿，所有内容均来自技能本体现状，未改动技能逻辑。
> 版本：`1.4.2`（本次仅补 `description` 中文名 + `tags` 元数据，无功能变化，沿用 1.4.2）。

## 一、技能基本信息（已就绪，平台会自动从 SKILL.md frontmatter 读取）

| 字段 | 值 |
|---|---|
| 技术名 (name) | `skill-doc-audit` |
| 显示名称 | 技能文档审计 |
| 版本 (version) | `1.4.2` |
| 描述 (description) | 技能文档审计：审计技能文档与代码的一致性及静态质量，找出版本迭代造成的文档漂移与结构/安全/可运行性/依赖隐患——死链接、失效的命令行参数、退出码表不符、状态或配置项漏写、描述脱节，以及 frontmatter 不规范、硬编码密钥、脚本语法错误、外部依赖与运行平台未声明等。当你刚改完某个技能的脚本或配置、担心文档没跟上，或某个技能经历多次版本迭代后想做一次体检/质量检查/一致性校验时使用。可审计任意本地技能目录，也可批量审计全部已安装技能。 |
| license | MIT |
| author | Jett |
| tags | 文档审计, 技能体检, 安全审计, 质量检查, 静态分析 |

## 二、上架网页表单需手动填写字段（草稿，供录入 skillhub.cn）

| 表单字段 | 建议值 | 说明 |
|---|---|---|
| Slug | `skill-doc-audit` | 小写字母+数字+连字符，提交后**不可改**，建议与技术名一致 |
| 显示名称 | 技能文档审计 | ≤20 字，已定为上架中文名 |
| 技能分类 | 开发工具（或 效率） | 审计类工具，建议「开发工具」 |
| 标签 | 文档审计 / 技能体检 / 安全审计 / 质量检查 / 静态分析 | 对应 frontmatter `tags` |
| 权限声明 | 本地文件读取(Read)；命令执行(Bash 运行 audit_docs.py)；可选自动修复时的本地文件写入(Edit/Bash，含自动备份) | 如实声明，便于安全审核 |
| 计费模式 | 免费 | MIT、无调用计费 |
| 支持平台 | WorkBuddy（可选 QClaw / ima） | AI 搭子跨平台通用 |
| 图标 | 512×512 PNG | 已选 `icons/Flat_minimal_app_icon_for_a__s_2026-08-29T10-04-49.png`（蓝底文档+放大镜，契合「文档审计」） |

## 三、技能文件包（上传用）

- `dist/skill-doc-audit.zip`：含 `SKILL.md`（根目录）+ `scripts/audit_docs.py` + `references/checkers.md`
- 校验：包内已含新描述「技能文档审计：」与 `tags` 字段，版本 `1.4.2`

## 四、发布前检查清单

- [x] 质量达 SkillHub「可参赛」线（11 维均分 9.09）
- [x] 对话触发率实测：正例 3/3 = 100%，反例误触发 0/2 = 0%
- [x] `description` 含中文名 + 关键词
- [x] `tags` 已补进 frontmatter
- [x] `dist` 重打包含新描述 + tags
- [x] 备份快照 `backups/skill-doc-audit.v1.4.2-pre-tags/` 可读可还原
- [x] 图标 PNG 选定并上传
- [ ] 账号实名认证确认（部分来源要求发布前完成）
- [ ] 登录 skillhub.cn → 发布页填写并提交审核

## 五、审核须知

- 提交后三线并行安全审核：内容合规过滤 / 科恩实验室深度漏洞扫描 / 云鼎实验室 AI 模型安全评估
- 审核周期约 1–7 个工作日，结果邮件 + 开发者后台通知
- 上架后接受 TRACE 评测体系评估
- 若被拒：按理由修改后可重新提交

## 六、关联产物（本项目目录）

- 图标候选：`icons/`（已选定 `Flat_minimal_app_icon_for_a__s_2026-08-29T10-04-49.png`，另两张已删除）
- 备份：`backups/skill-doc-audit.v1.3.0/`、`backups/skill-doc-audit.v1.4.2/`、`backups/skill-doc-audit.v1.4.2-pre-tags/`
